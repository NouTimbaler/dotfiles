#!/bin/sh

case "$(printf "Poweroff\\nRestart\\nLock" | dmenu -m 0 -fn "-20" -c -bw 3 -l 3 -i -p "Power option:")" in
	
	"Poweroff") sudo -- poweroff -f ;;
	"Restart")  sudo -- reboot -f ;;
    "sLock")    slock -m "$(fortune)";;
    "xtrLock")  xtrlock ;;

esac

